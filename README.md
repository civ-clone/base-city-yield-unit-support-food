# base-city-yield-unit-support-food

A value object for storing the value of `Unit` support costs related to `Food`.

Results in a `NegativeYield` (which negates all the values `add`ed, `set` and `subtract`ed).
